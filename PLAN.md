# PLAN.md — 클린코드 리팩토링 계획

`assemble.py`를 기능 변경 없이(behavior-preserving) 클린코드 원칙에 맞게 리팩토링하기 위한 계획.
각 단계마다 `pytest`(`tests/test_assemble.py`)가 계속 통과해야 하며, 테스트가 내부 구조(전역 변수 `q0~q4`, 함수명)에 의존하는 부분은 리팩토링과 함께 갱신한다.

## 목표

- 전역 변수 상태를 제거하고 명시적인 데이터 구조(예: dataclass)로 대체
- 매직 넘버/중복된 조건문을 제거하고 단일 진실 공급원(single source of truth)으로 통합
- UI(입출력/메뉴 표시)와 도메인 로직(호환성 검사, 조립)을 분리
- Enum을 사용해 타입 안정성 확보 (현재는 `int` 상수 + 비교로만 구분)
- 함수 하나가 한 가지 역할만 하도록 책임 분리

## 현재 구조의 문제점

1. **전역 변수 상태(`q0~q4`)**: 함수들이 전역 변수를 직접 읽고 쓰기 때문에 순서 의존성이 생기고 테스트 시 매번 초기화가 필요함.
2. **호환성 규칙 중복**: `is_valid_check()`(RUN용)와 `test_produced_car()`(TEST용)에 동일한 5가지 규칙이 각각 다른 형태(bool 반환 vs 메시지 출력)로 중복 구현되어 있어, 규칙 변경 시 두 곳을 모두 고쳐야 함.
3. **RUN과 TEST의 판정 기준 불일치**: 고장난 엔진(엔진값 `4`) 케이스가 `run_produced_car()`에는 있지만 `test_produced_car()`의 규칙에는 없음 — 사양인지 버그인지 불명확.
4. **매직 넘버/타입 안정성 부재**: `SEDAN=1`, `GM=1`, `MANDO=1`, `BOSCH_S=1`처럼 서로 다른 카테고리의 상수가 값이 겹쳐 실수로 잘못 비교해도 타입 체커가 잡아주지 못함.
5. **UI와 로직이 얽혀 있음**: `select_*` 함수들이 상태 갱신과 안내 문구 출력을 동시에 하고, `run_produced_car`/`test_produced_car`도 판정과 출력이 뒤섞여 있어 단위 테스트 시 항상 `capsys`로 문자열을 assert해야 함.
6. **`is_valid_range`의 반복 구조**: `if step == 0: ... if step == 1: ...` 형태로 매 단계 유사한 코드가 반복됨 (elif가 아닌 if 나열이라 버그 소지도 있음).
7. **`main()`의 낮은 테스트 가능성**: `input()`/`time.sleep()`이 함수 내부에 직접 섞여 있어 통합 테스트 작성이 어려움.

## 리팩토링 단계

### 1단계 — 도메인 모델 도입 (Enum + dataclass)

- `CarType`, `Engine`, `Brake`, `Steering`을 `enum.Enum`(또는 `IntEnum`)으로 정의해 기존 정수 상수를 대체.
- 조립 중인 차량의 선택 상태를 담는 `CarConfig` dataclass 도입 (`car_type`, `engine`, `brake`, `steering`, 모두 `Optional`로 시작).
- 전역 변수 `q0~q4` 제거 → `CarConfig` 인스턴스를 명시적으로 함수 간에 전달.

### 2단계 — 호환성 규칙 단일화

- 5가지 부품 호환성 규칙 + 고장난 엔진 규칙(총 6가지)을 `(설명 문자열, 조건 함수)` 형태의 리스트(또는 규칙 dataclass 리스트)로 정의해 한 곳에서만 관리.
  예: `RULES = [Rule("Sedan에는 Continental제동장치 사용 불가", lambda c: c.car_type == SEDAN and c.brake == CONTINENTAL), ..., Rule("엔진이 고장나있습니다.", lambda c: c.engine == BROKEN_ENGINE)]`
- 고장난 엔진은 "호환성 문제"가 아니라 "부품 결함"이므로 별도 성격의 규칙이지만, RUN/TEST 양쪽에서 동일하게 검사되도록 같은 규칙 리스트에 포함시켜 단일 진실 공급원을 유지한다.
- `is_valid_check()`와 `test_produced_car()`는 이 규칙 리스트를 순회하는 공통 함수(`find_violations(config) -> list[str]`)를 호출하도록 변경.
  - RUN(`run_produced_car`): 위반 목록이 비어있으면 조립 진행, 아니면 기존처럼 "자동차가 동작되지 않습니다"(또는 고장난 엔진 전용 문구) 출력. 위반 사유 목록 자체를 상세히 보여줄지는 기존 RUN 출력 형식 유지.
  - TEST(`test_produced_car`): 위반이 하나도 없으면 `PASS`, 하나라도 있으면 위반된 **모든** 규칙의 사유를 나열하고 `FAIL` 처리(기존: 첫 번째 사유만 출력 → 변경: 전체 위반 목록 출력).

### 3단계 — UI/도메인 로직 분리 (Enum으로부터 메뉴 자동 생성)

- `select_*` 함수를 "상태 갱신"과 "안내 문구 반환"으로 분리: 상태 갱신은 순수 함수(`config.car_type = ...`), 문구는 별도 매핑 딕셔너리(`CAR_TYPE_LABELS = {CarType.SEDAN: "Sedan", ...}`)로 조회해 출력.
- `run_produced_car`/`test_produced_car`를 "판정 로직(순수 함수, 반환값 있음)"과 "출력(print)" 두 계층으로 분리 → 순수 로직은 문자열 assert 없이 값으로 테스트 가능해짐.
- `show_menu`의 옵션 목록(`"1. Sedan"`, `"2. SUV"`, `"3. Truck"` 등)을 하드코딩된 개별 print문 대신, 각 단계별 **Enum 멤버 순서 + 라벨 딕셔너리를 순회하며 자동 생성**하도록 변경.
  예: `for i, member in enumerate(CarType, start=1): print(f"{i}. {CAR_TYPE_LABELS[member]}")`
  → 이렇게 하면 새 차량 타입을 `CarType` Enum에 멤버 하나 추가하고 라벨만 등록하면 메뉴 문구가 자동으로 갱신됨 (별도로 `show_menu`를 손댈 필요 없음).
- "고장난 엔진"처럼 항상 마지막 고정 옵션으로 두고 싶은 특수 항목은 Enum 순회 결과 뒤에 별도로 덧붙이는 방식으로 분리해 자동 생성 로직과 충돌하지 않게 한다.

### 4단계 — 입력 검증 정리 (Enum 멤버 수 기반 범위 계산)

- `is_valid_range`의 `if/if/if` 나열을 `step`별 허용 범위 딕셔너리로 대체하되, 범위 값을 하드코딩하지 않고 **해당 단계에 연결된 Enum의 멤버 수에서 자동 계산**한다.
  예: `STEP_RANGES = {0: range(1, len(CarType) + 1), 1: range(0, len(Engine) + 1 + 1), ...}` (엔진의 경우 "고장난 엔진" 같은 Enum 외 특수 옵션이 있다면 그 개수만큼 명시적으로 가산)
- 이렇게 하면 `CarType`에 멤버를 추가할 때 `STEP_RANGES`를 손으로 맞출 필요가 없어져, Enum과 범위가 어긋나는 실수를 원천 차단한다.
- 에러 메시지도 단계별 딕셔너리로 관리해 단계 추가/변경 시 한 곳만 수정하면 되도록 함.

### 5단계 — `main()` 루프 테스트 가능성 개선

- `input()`과 `time.sleep()` 호출을 얇은 wrapper(`delay`는 이미 분리되어 있음, `input()` 호출부도 동일하게 분리)로 감싸 의존성 주입 가능하게 함.
- 단계 전이(step transition) 로직을 별도 함수로 뽑아 `main()`은 "입력 받기 → 단계 전이 함수 호출 → 화면 표시" 뼈대만 남기고, 단계 전이 자체는 순수 함수로 테스트.

### 6단계 — 타입 힌트 및 정리

- 모든 공개 함수에 타입 힌트 추가.
- 이제 불필요해진 최상단 정수 상수(`SEDAN = 1` 등)는 Enum으로 완전히 대체 후 제거.

## 확정된 사양 변경사항

기존 동작과 달라지는 부분이므로 리팩토링과 함께 명시적으로 반영하고, 관련 테스트도 새 동작 기준으로 갱신한다:

- **고장난 엔진의 TEST 반영**: 기존에는 `test_produced_car()`가 고장난 엔진(엔진==4)을 FAIL 사유로 검사하지 않았음 → 이제 RUN과 동일하게 TEST에서도 "엔진이 고장나있습니다" 등을 위반 사유로 포함해 FAIL 처리한다.
- **TEST 위반 목록 전체 출력**: 기존에는 여러 규칙을 동시에 위반해도 `test_produced_car()`가 첫 번째 사유만 출력했음 → 이제 위반된 모든 규칙의 사유를 전부 나열한 뒤 FAIL로 판정한다 (예: Sedan+Continental+BoschB+Mobis처럼 2개 이상 규칙을 위반하면 두 사유 모두 출력).

이 두 변경으로 인해 기존 `tests/test_assemble.py`의 다음 테스트는 리팩토링 시 함께 갱신 필요:
- `test_test_produced_car_fail_*` 각 케이스: 단일 사유 문자열만 확인하던 assert를 "해당 사유가 출력에 포함되는지"로 유지하되, 복수 위반 케이스에 대한 신규 테스트(예: Sedan+Continental+BoschB+Mobis → 2개 사유 모두 출력) 추가.
- 고장난 엔진 + TEST 조합에 대한 신규 테스트 추가 (예: 엔진==4 단독일 때 TEST가 FAIL과 고장 사유를 출력하는지, 엔진==4와 다른 위반이 동시에 있을 때 두 사유가 모두 나오는지).

## 새 차량 타입(또는 부품) 추가 시 체크리스트

리팩토링 후에는 아래 항목만 확인하면 새 차량 타입을 추가할 수 있어야 한다. 메뉴 문구·입력 범위는 자동으로 갱신되지만, 호환성 규칙은 비즈니스 판단이 필요하므로 여전히 사람이 검토해야 한다.

1. `CarType` Enum에 새 멤버 추가.
2. `CAR_TYPE_LABELS`에 새 멤버의 표시 문구 추가.
3. (자동 반영됨, 확인만) `show_menu` 옵션 목록과 `is_valid_range`의 허용 범위가 Enum 멤버 수를 따라가는지 테스트로 확인.
4. `RULES` 목록을 검토해 새 차량 타입이 기존 엔진/제동장치/조향장치와 호환되지 않는 조합이 있는지 도메인 담당자와 확인 후, 필요한 규칙을 추가.
5. `tests/test_assemble.py`에 새 타입에 대한 `is_valid_check`/`test_produced_car`/`run_produced_car` 테스트 케이스 추가.

엔진/제동장치/조향장치를 추가할 때도 동일한 절차(Enum 추가 → 라벨 추가 → 규칙 검토 → 테스트 추가)를 따른다.

## 완료 기준

- 기존 `tests/test_assemble.py`의 테스트를 위 확정된 사양 변경에 맞게 갱신하고, 신규 테스트(복수 위반, 고장난 엔진+TEST)를 추가한 뒤 전부 통과.
- `pytest --cov=assemble` 기준 커버리지가 현재 수준(78%) 이상 유지.
- 전역 변수(`q0~q4`)가 코드에서 완전히 제거됨.
- 호환성 규칙(부품 호환성 5가지 + 고장난 엔진 1가지)이 코드베이스에 한 곳(단일 리스트/함수)에서만 정의됨.
- 새 차량 타입을 추가할 때 `show_menu`와 `is_valid_range`를 직접 수정하지 않아도 되는지(Enum 멤버 추가만으로 메뉴·범위가 갱신되는지) 테스트로 검증됨.
